#!/usr/bin/env ruby

$: << File.join(File.dirname(__FILE__), "../lib")
require 'mastermind'

Mastermind::Log.level = :debug

plot = Mastermind::Plot.new({})
plot.instance_eval(File.open(File.expand_path(ARGV[0])).read)
plot.execute