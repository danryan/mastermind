#!/usr/bin/env ruby

$: << File.join(File.dirname(__FILE__), "../lib")
require 'mastermind'

plot = Mastermind::Plot.new("ec2")
plot.instance_eval(File.open(File.expand_path(ARGV[0])).read)

plot.tasks.each do |task|
  puts task.inspect
end
  